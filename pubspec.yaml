name: orbitsync
description: Offline-first synchronization toolkit for Flutter apps with storage adapters, realtime transport, CRDT utilities, widgets, and devtools.
version: 0.1.0
homepage: https://github.com/Anu-Code07/offline-sync
repository: https://github.com/Anu-Code07/offline-sync
issue_tracker: https://github.com/Anu-Code07/offline-sync/issues
documentation: https://github.com/Anu-Code07/offline-sync/tree/main/docs
authors:
  - Anurag
environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

topics:
  - offline
  - sync
  - local-first
  - realtime
  - flutter

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: any
