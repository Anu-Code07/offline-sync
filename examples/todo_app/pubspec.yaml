name: orbitsync_todo_app
description: Todo sample demonstrating offline optimistic updates with OrbitSync.
publish_to: none
environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  orbitsync:
    path: ../..
