name: orbitsync_collaborative_notes_app
description: Collaborative notes sample demonstrating conflict-friendly local-first editing.
publish_to: none
environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  sync_core:
    path: ../../packages/sync_core
  sync_crdt:
    path: ../../packages/sync_crdt
  sync_flutter:
    path: ../../packages/sync_flutter
  sync_storage:
    path: ../../packages/sync_storage
