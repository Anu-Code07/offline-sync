{
  "name": "orbitsync-server-mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mock OrbitSync backend with latency, conflicts, packet drops, and websocket events.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "ws": "^8.20.1",
    "zod": "^4.4.3"
  }
}
