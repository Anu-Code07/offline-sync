name: sync_flutter
description: Flutter integrations for OrbitSync including widgets, controllers, background sync hooks, and state-management bridges.
version: 0.1.0
repository: https://github.com/example/orbitsync

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  sync_core:
    path: ../sync_core

dev_dependencies:
  flutter_test:
    sdk: flutter
