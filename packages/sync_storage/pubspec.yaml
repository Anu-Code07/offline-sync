name: sync_storage
description: Storage adapters for OrbitSync including in-memory, SQLite, Hive, Isar, and custom key-value backends.
version: 0.1.0
repository: https://github.com/example/orbitsync

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  sync_core:
    path: ../sync_core

dev_dependencies:
  test: any
