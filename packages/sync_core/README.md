# sync_core

Core OrbitSync primitives:

- `SyncEngine`
- `SyncCollection`
- `StorageAdapter`
- `SyncTransport`
- retry and dead-letter queues
- conflict resolvers
- delta patch calculation
- connectivity, security, realtime, and background sync contracts

This package has no Flutter or database dependency.
