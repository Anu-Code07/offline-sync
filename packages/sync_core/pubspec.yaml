name: sync_core
description: Core offline-first synchronization engine, contracts, retry queues, conflict handling, and delta sync primitives.
version: 0.1.0
repository: https://github.com/example/orbitsync
issue_tracker: https://github.com/example/orbitsync/issues

environment:
  sdk: ">=3.4.0 <4.0.0"

topics:
  - offline
  - sync
  - local-first
  - realtime
  - flutter

dev_dependencies:
  test: any
