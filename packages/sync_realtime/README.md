# sync_realtime

Realtime connector package for OrbitSync.

Features:

- websocket-style socket abstraction
- auto reconnect
- heartbeat ping/pong
- room and channel subscriptions
- presence messages
- event streaming into `SyncEngine`
